name: Qodana Code Analysis

on:
  push:
    branches: [ "main", "dev" ]
  pull_request:
    branches: [ "main" ]
  workflow_dispatch:

jobs:
  qodana-scan:
    runs-on: ubuntu-latest
    permissions:
      contents: write
      pull-requests: write
      checks: write
      security-events: write

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Run Qodana Scan
        uses: jetbrains/qodana-action@v2024.1
        # İŞTE KİLİT NOKTA: Hata verse bile sistemi çökertmeyi yasaklıyoruz!
        continue-on-error: true 
        env:
          QODANA_TOKEN: ${{ secrets.QODANA_CONFIGURATIONS_TOKEN }}
        with:
          # Python müfettişini çağırıyoruz
          args: --linter,jetbrains/qodana-python-community:2024.1
